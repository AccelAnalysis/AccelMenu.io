# SQLite database path or connection string
DATABASE_URL="file:./dev.db"

# API key required for non-GET routes (sent via x-api-key header)
API_KEY="changeme"

# Port for the Express server
PORT=4000
